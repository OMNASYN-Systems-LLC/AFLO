{
  "name": "@aflo/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Auth provider adapter (Clerk accepted, ADR-0006; activation founder-gated on credentials). Provider-neutral session contract, fail-closed guards, and the demo provider for the synthetic prototype. Role/membership authority stays with organization_members.",
  "exports": {
    ".": "./src/index.ts",
    "./webhook": "./src/webhook.ts",
    "./invitation-token": "./src/invitation-token.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.10.2",
    "eslint": "^9.39.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.64.0",
    "vitest": "^4.1.10"
  }
}
