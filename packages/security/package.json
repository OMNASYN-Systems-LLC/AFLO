{
  "name": "@aflo/security",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Security primitives: canonical JSON serialization, SHA-256 digests, Ed25519 asymmetric signing/verification, and cryptographically verifiable handoff packages (tamper-evident, scoped, expiring, revocable). No production key material lives in the repo.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.10.2",
    "eslint": "^9.39.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.64.0",
    "vitest": "^4.1.10"
  }
}
