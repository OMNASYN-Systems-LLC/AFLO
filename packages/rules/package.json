{
  "name": "@aflo/rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AFLO deterministic rules kernel: versioned lifecycle, readiness, and engagement rules with a metadata registry. Zero runtime dependencies; no probabilistic input.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^9.39.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.64.0",
    "vitest": "^4.1.10"
  }
}
