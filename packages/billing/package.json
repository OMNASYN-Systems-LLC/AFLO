{
  "name": "@aflo/billing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AFLO deterministic billing kernel: invoice/subscription/payment state machines and the subscription-access entitlement gate. Stripe-free and readiness-free; the Stripe integration and persistence layers consume it later.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@aflo/rules": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^9.39.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.64.0",
    "vitest": "^4.1.10"
  }
}
