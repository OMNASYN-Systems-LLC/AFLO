# ΛFLO Build Status

Living status of the ΛFLO (technical: AFLO) V1 build. Updated every implementation cycle. Newest state at top of each list.

_Last updated: 2026-07-19 · PRs #2–#62 merged to `main`. Merged surface: client lifecycle A–P, full Drizzle persistence schema (core + client-workflow + sibling-package/AI tables, PRs #41–#44), notifications kernel+wiring+preferences, auth boundary, round-up simulator, goals, ΛFLO brand, Wealth Academy, partner referrals + neutrality, signed handoff packages, provider-neutral credit-data interfaces (#41), vendor-discovery registry + disabled Experian adapter (#46), the Financial Resolution Concierge substrate + credit-report display card + Opportunity & Risk registry (#48–#52), **fail-closed RLS org-isolation now RUNTIME-VERIFIED credential-free via PGlite (#45, non-superuser role proves list/direct-id/nested isolation + fail-closed)**, and the **first durable PostgreSQL repository — the transactional outbox (#53, `DrizzleOutboxRepository`) with crash recovery, `FOR UPDATE SKIP LOCKED`, and a tested RLS deployment contract, proven on PGlite**. Founder decisions applied: Clerk accepted (ADR-0006), Neon provisioned, Control Plane Delta brand, **ΛFLO display brand + expanded charter (`FOUNDER_DIRECTIVE_2026-07-18.md`)**, plus the strategic reframing to a governed **Financial Resolution Concierge** with all external providers behind mock/discovery adapters until contracted (ADR-0011). Vercel per-PR previews are live (project `aflo-web`). PGlite (in-memory Postgres) is now the credential-free proving ground for RLS and repositories; the remaining decisive milestone is the **live Neon connection + store swap** (credential-gated on `DATABASE_URL`). Credential-gated items paused; safe durable-persistence + deterministic work continuing._

_**Production Conversion phase (founder directives 2026-07-19).** The founder set the production-conversion order: complete secure messaging (Phase 1) → production runtime fail-closed contract → Neon repository swap → Clerk → permission-based authorization → RLS request context → files → calendar → email → worker → Concierge → budget tools. **Unblocked and in progress:** Phase 1 messaging completion (store wiring #59, UI #60, interactive send #62, read receipts + unread #63), the production runtime fail-closed contract (#64) now **actively enforced at boot** (#65, `apps/web` instrumentation refuses to start a misconfigured production), and — under the founder's expanded **Auth/Authz/Production-Runtime Conversion directive (2026-07-19)** — the credential-free auth foundation: the deterministic **authorization engine core** (ADR-0018, in PR), then session context, Clerk webhook verification, invitations, demo-runtime removal, the Postgres repository factory gates, and RLS request-context — all buildable and testable now so activation is instant when Clerk keys + Neon URLs arrive. **Credential-blocked (stop the workstream, keep the seam):** Neon connection/repositories/RLS-request-context (`DATABASE_URL`/`DIRECT_DATABASE_URL`), Clerk (keys + webhook secret), private storage, Resend, Railway worker, Stripe. Non-negotiable: outside test mode ΛFLO must fail closed, never silently fall back to demo identity, mock repositories, in-memory state, synthetic data, mock delivery, public storage, preview DB, or ephemeral production keys._

## Founder implementation order (2026-07-18) — status

1. Lead→client conversion ✅ · 2. Structured intake ✅ · 3. Neon/Drizzle persistence — schema ✅ (PRs #41–#44), **durable outbox repository ✅ (#53, PGlite-proven)**, **connection ⛔ `DATABASE_URL`**, store swap ⏭ · 4. Authz/RLS/audit/tenant isolation — app-level audit + tenant tests ✅, **RLS DDL merged + RUNTIME-VERIFIED ✅ (#45, PGlite non-superuser)** · 5. Readiness rules ✅ · 6. Roadmap + monthly actions ✅ · 7. Clerk auth — boundary ✅, **activation ⛔ keys** · 8. Outbox worker — repository ✅ (#53), **worker process ⛔ (needs shared DB + Railway)** · 9. Notification preferences ✅ · 10. Resend adapter — ⛔ keys (test-mode buildable) · 11. Twilio SMS — ⛔ keys · 12. Wealth Academy ✅ · 13. Reports ✅ + signed handoff packages ✅ · 14. Stripe test-mode ⛔ keys · 15. Partner referrals ✅ · 16. Partner neutrality ✅ · 17. **Credit-data interfaces ✅ (PR #41 — provider-neutral, mock only)** · 18. **Behavioral Support (opt-in) ⏭** · 19. Opportunity registry ✅ (#51–#52) · 20. Pilot 👤 · 21. Provider eval 👤. · **Strategic-reframing add-ons:** vendor-discovery registry + disabled Experian adapter ✅ (PR #46, ADR-0011); Resolution Concierge substrate + credit-report card (#48–#50).

## Legend

`✅ done` · `🔨 in progress` · `⏭ next` · `⛔ blocked` · `🕓 deferred` · `🔒 security review required` · `👤 founder decision required`

---

## Completed ✅

- **Slice A — domain event model** (`packages/shared/src/events`): 25 versioned lifecycle event contracts with exhaustive payload map, validating fail-closed factory, correlation/causation threading, deterministic serialization (11 tests). **Merged (PR #18).**
- Founder decisions applied (**merged PR #17**): ADR-0006 Accepted (Clerk), Neon provisioned status, two-level brand system doc.

- Monorepo scaffold (pnpm workspaces): `apps/web`, `apps/worker`, `packages/{config,rules,ai,shared}` + six documented stub packages.
- Deterministic rules kernel (`@aflo/rules`): lifecycle stages, readiness-stage rules (`readiness.v1.0.0`), engagement rules (`engagement.v1.0.0`), rule metadata registry (stable id, version, effective date, inputs/output, reason codes, change history) with lockstep tests.
- AI boundary (`@aflo/ai`): 12-agent roster, charter output envelope; compliance-guard hard-stop semantics.
- Domain + repositories (`@aflo/shared`): typed model, repository interfaces, in-memory mock implementations over synthetic Golden Key data (fixed demo clock), org-scoped record-by-record with a foreign-org leak regression test.
- Staff portal visual slice: sign-in shell, dashboard (KPIs, stage distribution, pipeline, needs-attention, appointments), lead/client list, client detail (stage + reason codes + rule version, roadmap, monthly actions, documents, appointment, engagement, quarterly report preview, AI drafts with confidence + review status).
- Documentation set reconciled to the Product Charter: charter, V1 scope, database schema proposal, authorization matrix (incl. Organization Admin), agent boundaries, ADR-0001…0006, compliance baseline, Golden Key workflow discovery questions, design system, deployment notes.
- CLAUDE.md reconciled to the 12-agent contract, expanded stack, Organization Admin role, and auto-merge policy.
- CI (GitHub Actions): typecheck, lint, unit tests, build, **critical Playwright e2e (5)**, informational dependency audit.
- Env examples (`apps/web`, `apps/worker`) — placeholders only.
- Deployment config-as-code: `apps/web/vercel.json`, `apps/worker/railway.json`; `DEPLOYMENT.md` updated with the account-authorization blockers.
- **PR #2 merged to `main`** — `main` contains the deployable monorepo (merge commit `1d76772`).
- Deterministic billing kernel (`@aflo/billing`, `billing.v1.0.0`): invoice/subscription/payment allow-list state machines with reason codes, `isInvoicePastDue`, the grace-aware subscription-access entitlement gate, and a billing rule registry — pure, Stripe-free, readiness-free (20 tests). **Merged (PR #3).**
- Partner-orchestration roadmap captured as an authoritative founder decision (`PARTNER_ORCHESTRATION_ROADMAP.md`), with ADR-0007, compliance/scope reconciliation, and five gated stub packages (`academy`, `partner-marketplace`, `credit-data`, `opportunity-intelligence`, `embedded-finance`) + `integrations` stub. **Merged (PR #16).**

## In progress 🔨

- **Production Cutover PHASE 2 — auth persistence schema** (founder cutover directive, ADR-0026): forward-only **migration 0005** (offline-generated, credential-free) adds the identity bridge — `identity_provider_accounts` (Clerk→AFLO user), `invitations` (org-scoped, `token_digest` sha256 only, unique `(org,token)` + partial-unique pending `(org,email)`), `client_user_links` (org-scoped, partial-unique active-client + active-user), `provider_webhook_events` (`(provider,event_id)` idempotency, `payload_digest` only), `session_revocations` (user-scoped, nullable org). 6 new enums. **RLS**: `invitations` + `client_user_links` get FORCE RLS (migration-0003 fail-closed pattern); `identity_provider_accounts`/`provider_webhook_events`/`session_revocations` are intentionally un-scoped (auth-resolver reads them before/across org context, like users/rule_versions) — encoded as an explicit `RLS_EXEMPT_TENANT_TABLES` allowlist. **Proven on PGlite** (non-superuser role): invitations isolation (list/direct-id/write-reject/fail-closed), token+pending-email uniqueness, client-user double-claim rejection. Additive-only, no destructive statements. **94 database tests.** ⛔ **Applying to Neon dev/preview is credential-gated** (`DATABASE_URL` not in the CI/agent environment) — GitHub remains the migration authority; the migration is committed + ready for the deploy pipeline's `db:migrate`. Messaging tables (conversation_threads/messages) follow in 0006. _In PR._
- **Auth/Authz Conversion — RLS per-request tenant context** (founder directive PHASE 8, ADR-0025): `@aflo/database` gains `withOrgContext(db, organizationId, work)` — opens a transaction and sets `app.current_org_id` **TRANSACTION-LOCAL** (`set_config(..., is_local=true)`), so RLS (migration 0003) scopes every query to that org and the setting **reverts at COMMIT/ROLLBACK** — a pooled connection can never carry one request's org into the next (the leak a session-level `SET` would cause). Fails closed (`TenantContextError`) on an empty org; tenant-scoped path only (worker/AI use an RLS-bypassing role; platform-admin cross-tenant is a separate audited surface). **Proven credential-free on PGlite** under a non-superuser role — 5 tests: in-transaction scoping, **no context leak onto the connection after the tx** (the key property), no cross-transaction leak, cross-tenant write rejection (RLS `WITH CHECK`), fail-closed-on-empty. Driver-agnostic handle → the PGlite-proven path runs against Neon in production. 87 database tests. **Not yet wired** — routing handlers through `withOrgContext` + Drizzle repositories on a live connection is gated on Neon `DATABASE_URL` + a non-superuser runtime role. _In PR._
- **Auth/Authz Conversion — account status + session revocation** (founder directive: disabled-account handling + session revocation, ADR-0024): `@aflo/auth` gains `account.ts` (pure, barrel) — `AccountState` + `disableAccount` (active→disabled AND revokes all live sessions via a cutoff), `reactivateAccount` (leaves the cutoff — old sessions stay dead), `revokeAllSessions` ("sign out everywhere"), and `isSessionRevoked` (fails closed on bad timestamps). **`buildSessionContext` now fails closed at the session layer**: a `disabled` account resolves to **null** (no session at all, not merely an `authorize()` denial), and a session issued before the account's `sessionsInvalidatedBeforeIso` cutoff resolves to null. `AfloIdentity` gains the optional cutoff; `SessionContextInput` gains optional `sessionIssuedAtIso` (backward-compatible). Defense in depth — disabled/revoked rejected at BOTH the session layer and the engine (`account_disabled`); tests assert both. 11 tests → **97 auth tests total**. **This completes the credential-free identity foundation.** Persisting `AccountState` + wiring the real session issued-at/cutoff is credential-gated (Clerk session + Neon). _In PR._
- **Auth/Authz Conversion — membership / role-change model** (ADR-0023): `@aflo/auth` gains `membership.ts` (pure, barrel) — `MembershipRecord` (the stored `organization_members` row; distinct from the session projection) + `applyAcceptedBinding` (invitation `AcceptedBinding` → a staff **membership**, a client **link**, or a **rejection** for unbindable roles / missing client) + pure transitions `changeMemberRole`/`revokeMembership`/`reinstateMembership` with stable `MembershipDenial` reasons, deny-by-default, input never mutated. Plus `memberRoleFromRole` (roles.ts, inverse of `roleFromMemberRole`). This is the **shared reconciliation target** tying invitation acceptance (ADR-0022), Clerk `organizationMembership.*` webhooks (ADR-0020), and the session context (ADR-0019) together. Authorization of WHO may change/revoke stays with `authorize()` (owner-reserved); org-wide invariants (≥1 owner) are a service-layer concern (documented). 9 tests → **85 auth tests total**. **Not yet wired** — the webhook handler + accept route + Drizzle `organization_members` writes are credential-gated. _In PR._
- **Auth/Authz Conversion — invitation state machine** (founder directive PHASE 5, ADR-0022): `@aflo/auth` gains the deterministic, credential-free invitation lifecycle — `invitation.ts` (pure, in the barrel): `Invitation` record + `issueInvitation`/`acceptInvitation`/`revokeInvitation`/`expireInvitation` returning a discriminated `InvitationResult` with stable denial codes; injectable `nowIso`; expiry fails closed. **Identity-claiming invariant enforced** (founder: "one Clerk identity cannot claim another client record"): a client invite reserves a specific `reservedClientId`, a staff invite must not; `acceptInvitation` sources the `AcceptedBinding` (org/role/client) **from the invitation, never the accepting request** — a substituted `claimedClientId`/`claimedOrganizationId` is rejected (`client_mismatch`/`org_mismatch`), and acceptance requires the verified email to match. Token security behind the server-only `@aflo/auth/invitation-token` subpath (node:crypto): `generateInvitationToken`/`hashInvitationToken`/constant-time `verifyInvitationToken` — only the hash is stored, the raw token lives only in the invite link. 12 tests → **75 auth tests total**. **Not yet wired** — the Clerk-invite send + Drizzle `invitations` table + accept route are credential-gated. _In PR._
- **Auth/Authz Conversion — demo-identity marker CI guard** (founder directive PHASE 6, ADR-0021): `scripts/check-demo-markers.mjs` (run in the `verify` job via `pnpm check:demo-markers`, after lint) scans `apps/*/src` + `packages/*/src` for precise demo-identity/session markers (`DemoAuthProvider`, `demo-user`, `demo-client`, `portal-demo-client`, `mock-session`, `synthetic-session`, `demo-staff`, `demo-org`; NOT the noisy word "hardcoded"), exempts test files, and **fails the build** on any marker outside an explicit **ALLOWLIST**. The allowlist names the one demo runtime path today — `packages/auth/src/demo.ts` (the `DemoAuthProvider` class) + `apps/web/src/lib/data.ts` (its composition root) — and is the **burn-down list that must reach zero when Clerk activates**. Mechanically enforces the non-negotiable "no demo identity outside tests"; static complement to the boot-time runtime contract (ADR-0017). Verified both directions (passes clean; fails on an injected marker). _In PR._
- **Auth/Authz Conversion — Clerk webhook signature verification** (founder directive PHASE 3, ADR-0020): `@aflo/auth/webhook` (a **server-only subpath**, node:crypto kept out of the client barrel) — `verifyWebhook` recomputes the Svix HMAC over `${svix-id}.${svix-timestamp}.${rawBody}` and constant-time-compares (`timingSafeEqual`) each `v1` signature; fails closed with stable reasons (missing_headers / malformed_secret / invalid_timestamp / timestamp_out_of_tolerance ±300s / no_signatures / signature_mismatch), `nowSeconds` injectable for deterministic tests, secrets never in errors. Plus a typed 8-type `WebhookEvent` union (unknown→`unhandled`, never throws), an idempotency contract (`WebhookEventRecord` keyed by Svix id + `WebhookDedupeStore`/`InMemoryWebhookDedupe`), and a `WebhookReconciler` stub. **Does NOT process unsigned webhooks.** 12 webhook tests (genuine accept, tampered/wrong-secret/missing-header/stale/future/malformed reject, multi-sig, every type parsed, idempotent replay) → **63 auth tests total**. **Not yet wired** — the `/api/webhooks/clerk` route (raw-body read → verify → dedupe → sync → audit) is credential-gated (needs `CLERK_WEBHOOK_SECRET` + the Drizzle `webhook_events` table). _In PR._
- **Auth/Authz Conversion — authenticated session context** (founder directive PHASE 2, ADR-0019): `@aflo/auth` gains the credential-free session-context layer that CONSUMES the authorization engine — `identity.ts` (ΛFLO's own `AfloIdentity`/`Membership`/`ClientLink` value types, deliberately NOT Clerk SDK objects; `clerkUserId` is an opaque reference; `isPlatformAdmin` only from the verified flag), `session-context.ts` (`SessionContext` carrying every PHASE 2 field — sessionId/clerkUserId/afloUserId/role/permissions/accountStatus/activeOrganizationId/activeMembershipId/membershipStatus/linkedClientId/assignedClientIds; a **server-only** `SessionContextProvider`; the deterministic `buildSessionContext` resolver with role precedence **platform-flag → staff membership → client link**; `toPrincipal` bridge to `authorize()`; and `requireSessionContext` fail-closed guard), and `test-provider.ts` (`TestSessionContextProvider` — tests only, NOT a runtime demo fallback). Browser NEVER authoritatively supplies org/user/client/membership/role/permissions — the resolver takes only verified records. 16 session tests (role resolution, precedence, permissions-from-role, account/membership carried through, PHASE 2→4 bridge, fail-closed guard) → **50 auth tests total**. **Not yet wired** — the Clerk-backed provider (credential-gated) + handler migration follow; existing StaffSession/ClientSession left intact. **PHASE 3 (Clerk webhook verify) next.** _In PR._
- **Auth/Authz Conversion — authorization engine core** (founder directive PHASE 4, ADR-0018): `@aflo/auth` gains a pure, deterministic, credential-free authorization core — `roles.ts` (six-role authorization vocabulary distinct from the domain membership role, with a `roleFromMemberRole` bridge), `permissions.ts` (the 34-permission `resource.action` union), `policies.ts` (role→permission map derived from `AUTHORIZATION_MATRIX §4`; deny-by-default; Owner=all, Admin=Owner−`manage_members`, Staff=operational set, Client=self-service only, Platform Admin=read-only cross-tenant subset+`audit.read`, Partner Viewer=reserved/empty), `denial-reasons.ts` (stable codes + sensitive-denial audit set), and `authorization.ts` — `authorize(request)→{allowed,reason}` evaluating **identity → account → membership → tenant → permission → ownership → assignment → consent → record-state** in fixed fail-closed order. Adversarial security review caught a fail-open (ownership gate fell through to ALLOW when `clientId` was absent); fixed by classifying permissions **client-scoped vs org-scoped** and failing closed on a missing `clientId` for client-scoped permissions (ownership + staff-assignment gates), plus copy-on-read policy sets, fail-closed unknown-role lookup, and a platform-admin read-only invariant test. Added Secure-messaging + Billing rows to `AUTHORIZATION_MATRIX §4` so all 34 permissions are matrix-traceable. 34 tests incl. cross-tenant, IDOR/ownership, **missing-clientId fail-closed**, unassigned-staff, revoked-membership, disabled-account, deny-by-default, platform-admin read-vs-mutate. **Not yet wired** — consumed by the session context (PHASE 2) next. _In PR._
- Client-lifecycle slice B: outbox typed contracts + deterministic claim/complete/fail rules (`outbox.v1.0.0`) + ADR-0008. **Merged (PR #19).**
- Slice C1: deterministic pipeline rules (`pipeline.v1.0.0`). **Merged (PR #20).**
- Slice C2 — staff lead conversion (commercial-grade mandate captured in `COMMERCIAL_GRADE_V1.md`): configurable pipeline stage ids + client status; mutable AfloStore applying rules-gated mutations with outbox events and an append-only audit trail; staff Lead Pipeline workspace with only rule-legal actions; server-side-session-only tenancy for mutations. **Merged (PR #21).**
- Slice D — client intake: deterministic intake-completeness rules (`intake.v1.0.0`); `IntakeSectionCompleted` event (26 types); store workflow with pipeline↔intake consistency gates in both directions; staff intake workspace. **Merged (PR #22).**
- Slice E — readiness assessment workflow: recorded assessment history with deterministic review gate (`review.v1.0.0`) and threshold-interpolated next actions; blocked attempts audited, never recorded. **Merged (PR #23).**
- Slice F — roadmap approval workflow: `roadmap.v1.0.0` allow-list state machine, `Roadmap` entity, `RoadmapPublished` event (27 types), approval stamping/withdrawal, rule-legal staff actions. **Merged (PR #24).**
- Slice G — monthly action plan workflow: `action.v1.0.0` status rules, manual creation with `TaskAssigned`, completion with `TaskCompleted`, rule-legal plan-card actions. **Merged (PR #25).**
- Slice H — quarterly report workflow: `report.v1.0.0` rules, deterministic generation from recorded facts only, `ProgressReportPublished` event (28 types), review-gated publication. **Merged (PR #26).**
- Slice I — client portal: `PortalView` published-only projection, fail-closed identity, `/portal` client shell, demo client session. **Merged (PR #27).**
- Slice J — staff workflow completion: `document.v1.0.0` review-state rules, `requestDocument`/`transitionDocument`/`scheduleAppointment`/`addNote` store workflows with `DocumentRequested`/`DocumentUploaded`/`DocumentReviewed`/`AppointmentScheduled` events, client-detail document/appointment/note controls. **Merged (PR #28).**
- Slice K — auth adapter boundary: `@aflo/auth` activated — provider-neutral session contract, fail-closed guards, demo providers; `apps/web` routed through the boundary; Vercel connected. **Merged (PR #29).**
- Slice L — notifications kernel: `@aflo/notifications` activated — `notification.v1.0.0` consent-gated delivery (consent gate, typed templates, delivery state machine, mock provider), 18 tests. **Merged (PR #30).**
- Slice M — notification wiring: the kernel wired into the store; workflow events plan consent-gated communications recorded in a staff-visible log (revoked-consent sends suppressed and recorded, never silent). **Merged (PR #31).**
- Slice N — Drizzle schema foundation: `@aflo/database` activated (ADR-0005) — kernel-derived Postgres enums + core tables matching the implemented model, offline-generated migration, 9 lockstep tests. **Merged (PR #32).**
- **Phase A1b — sibling-package + AI schema tables**: added 7 more Drizzle tables (partners, partner_referrals with inline neutrality jsonb, education_assignments, notification_preferences, communications, ai_runs agent-envelope provenance, handoff_packages) + 9 enums (partner/referral/notification/agent-name via tuple() from the source `as const` arrays; education/agent/ai-review-status canonical via indexed access) as a **forward-only additive** `0002` migration. Added the deferred `ai_run_id` FKs (roadmaps/quarterly_reports/education_assignments → ai_runs) via `ADD CONSTRAINT`. Critique fixes: handoff_packages `payload_digest` NON-unique (re-issue after revoke must not collide), `consent_scope` is a scope string (documented), source-widened `CommunicationLogEntry.channel` from `string` to `NotificationChannel` in `@aflo/shared` so the `communications.channel` enum column is type-sound. **The Drizzle schema now covers every implemented domain.** 35 database tests. **Merged (PR #44).** (A prior baseline-migration fix — the `schema` glob omitted `enums.ts`, so `0000` created only 1 of 14 enum types — landed as **PR #42** with a `migration.test.ts` guard; never applied, no live DB.)
- **Phase A1a — client-workflow schema tables**: extended `@aflo/database` with 13 Drizzle tables for already-implemented domains (readiness_assessments, financial_profiles, credit_profiles, goals, roadmaps, roadmap_milestones, monthly_actions, documents, appointments, quarterly_reports, notes, simulation_settings, virtual_transactions) + 7 kernel/canonical enums, as a **forward-only additive** `0001` migration (CREATE TYPE/TABLE/INDEX + FK ADD CONSTRAINT only — no drops). Tenant-isolation invariant enforced (organization_id on every table); money as integer cents (bigint); indexed-access enum lockstep; partial-unique primary goal; appointments carries no phantom status column; goals.target_date NOT NULL. Applied the design-workflow critique fixes. 24 database tests (lockstep + migration integrity + table invariants). Sibling-package tables (partners, referrals, education, notifications, communications, ai_runs, handoff) landed next in A1b; the Neon connection/repository swap follows. **Merged (PR #43).**
- **Fix — baseline migration integrity**: the generated baseline migration created only one enum type (`appointment_status`) while six others its own tables reference were never created, so it could not apply to a clean Postgres (the lockstep tests check schema-as-code, not the SQL, so it went unnoticed). Root cause: drizzle-kit only emits `CREATE TYPE` for enums discovered via the config `schema` glob, which omitted `enums.ts`. Fixed the glob, regenerated a clean baseline (all 14 enum types + 9 tables), and added a `migration.test.ts` that fails if any declared enum lacks a `CREATE TYPE` or any enum column references an uncreated type. Never-applied migration (no Neon connection) — safe to regenerate, not a destructive production migration.
- Slice O — round-up simulator: `roundup.v1.0.0` deterministic calculator (SIMULATION ONLY), simulation domain + rule-computed seed, store workflows, client-detail simulator card. **Merged (PR #33).**
- Slice P — goals workflow: store `createGoal`/`updateGoalProgress`/`setPrimaryGoal`, `GoalCreated` first producer, single-primary invariant, client-detail Goals card. **Merged (PR #34).**
- Slice Q — ΛFLO brand + charter reconciliation: `FOUNDER_DIRECTIVE_2026-07-18.md` authoritative; `AfloWordmark`/`PoweredByAflo` (ΛFLO display, AFLO accessible name); BRAND_SYSTEM §0; BUILD_STATUS reconciled to the 21-item order. **Merged (PR #35).**
- Slice R — notification preferences (founder order #9): channels + per-type routing + append-only preferences + `resolveDelivery` enforced before send; staff per-channel toggle panel. **Merged (PR #36).**
- **Slice S — ΛFLO Wealth Academy** (founder order #12): `@aflo/academy` activated from stub — versioned content catalog (courses→modules→lessons + ebooks/workshops, `contentVersion`, signed-playback media keys never raw URLs), deterministic `education.v1.0.0` trigger→lesson assignment (`selectEducation` + reason codes) and `scoreKnowledgeCheck` (fail-closed), Golden Key starter library; `EducationAssignment` domain with full provenance; store `assignEducation` (idempotent while open, `EducationAssigned` — first producer) + `completeEducation` (`EducationCompleted`, deterministic knowledge-check score) + `educationFor`; staff Education card on client detail; **portal Wealth Academy surface** (lesson titles + completion, no staff-internal reason codes). Completion is educational only — never gates a regulated product. 7 academy + 7 store tests + academy e2e. **Merged (PR #37).**
- **Slice T — signed verification handoff packages** (founder order #13, ADR-0009): `@aflo/security` activated — `security.v1.0.0` canonical JSON, SHA-256 digest (integrity, not a signature), **Ed25519 asymmetric** sign/verify with self-describing `keyId` (rotation seam), `HandoffPackage` + fail-closed ordered `verifyHandoffPackage` (revoked→digest→key→signature→expiry, specific verdicts). `HandoffFacts` payload is verified facts only — the **ΛFLO readiness stage** (`readinessIsBureauScore: false`, never a bureau score), primary goal, approved-document count, latest published quarter; no SSN/bank/raw-credit data. Store `generateHandoffPackage` fails closed on three gates (server-verified actor, active `partner_data_sharing` consent, a recorded assessment), signs from a dev-only per-process key, audits issuance; `verifyHandoffPackageById` (pure read) + one-way `revokeHandoffPackage` (audited). Staff "Verification handoff" card (generate/verify-verdict/revoke, shows digest + keyId); consent-gate hint when blocked. Type-only facade re-export keeps `node:crypto` server-side. Explicitly **not** audit-proof/legally-verified/underwriting-approved/zero-knowledge. 9 security + 10 store tests + handoff e2e (3). **Merged (PR #38)** — founder-reviewed for crypto key handling.
- **Slice U — tracked partner referrals + Partner Neutrality Engine** (founder order #15–#16, ADR-0010): `@aflo/partner-marketplace` activated from stub — `partner.v1.0.0` referral lifecycle allow-list (`suggested → shared_with_client → client_engaged → outcome_recorded`, `declined` from any non-terminal), the eight-field `NeutralityRecord` with fail-closed `validateNeutralityRecord`, and `orderPartnerOptions` (non-commercial-first then name, **never** by compensation). `PartnerReferral` domain record + synthetic partner directory (fictional names, dollar-free disclosures — ADR-0007). Store `createReferral` (fails closed without a complete neutrality record; emits `PartnerReferralCreated`), `transitionReferral`, `recordReferralOutcome` (staff observation, never an approval) + `partnersFor`/`referralsFor`; all audited, org/actor scoped, isolated from the readiness engine. Staff "Partner referrals" card (create with neutrality disclosure, route the lifecycle, record outcome). 9 marketplace + 8 store tests + partner-referrals e2e (2). **Merged (PR #39).**
- **Slice V — provider-neutral credit-data interfaces** (founder order #17, ADR-0007 §5): `@aflo/credit-data` activated from stub — `credit-data.v1.0.0` normalized bureau-agnostic model (`NormalizedCreditReport`, tradelines, inquiries, scores), the `CreditDataProvider` adapter interface (`isProduction` false for all V1 providers), deterministic `summarizeCreditReport` → readiness-relevant `CreditFacts` (utilization, derogatory count, trailing-year hard inquiries, on-time rate; null utilization guarded), and a synthetic `MockCreditDataProvider` + `syntheticCreditReport` (rejects unknown subjects, never fabricates). No production bureau, no FCRA-gated data, no readiness-stage derivation; `subjectRef` is a client id, never a real SSN. Pure library — store/UI wiring is a follow-up. 6 tests. **Merged (PR #41).**
- **Slice W — vendor-discovery registry + disabled Experian adapter** (strategic-reframing order: Experian disabled adapter skeleton + vendor-discovery registry, ADR-0011): `@aflo/integrations` activated from stub — `integrations.v1.0.0` frozen vendor-discovery registry as the machine-checked source of truth for external-vendor contract status (lifecycle `discovery → contract_pending → sandbox → production`; every seeded candidate — Experian Partner Solutions, CreditStrong, Ava, Acorns, Marqeta, Highnote — at `discovery`, `isEnabled: false`, `requiresAgreement: true`, with `trademarkOwner` recorded for attributable nominative use). Fail-safe helpers: `isVendorEnabled` (true only for production+enabled; unknown ⇒ false), `assertVendorEnabled` (throws `VendorNotEnabledError` — the guard every regulated path calls first), `validateVendorRegistry` (asserts the V1 invariant that NOTHING is enabled — CI fails if it ever regresses). Disabled `ExperianCreditDataProvider` implements `@aflo/credit-data`'s `CreditDataProvider` seam (real bureau impl drops in later behind the same interface) but holds no credentials, makes no network call, reports `isProduction: false`, and rejects every `fetchReport` with `ProviderNotContractedError` — never fabricates a report, never implies availability. Registry is internal engineering-planning data, **not** a client-facing partner list (nominative use only). The boundary "no external vendor until contracted + compliance-reviewed" is now executable and tested, not just prose. A 3-lens adversarial review hardened it: `validateVendors` is pure with negative-fixture tests proving it detects regressions; registry records are deep-frozen (a runtime write throws); the disabled Experian adapter declares no score models (never invents capabilities) and hardcodes `isProduction: false`; `toPublicVendorView` is the client-safe projection (id/domain/status only). 25 tests. **Merged (PR #46).**
- **Slice X — Financial Resolution Concierge substrate** (strategic reframing: governed understand→diagnose→organize loop, ADR-0012): `resolution.v1.0.0` kernel in `@aflo/rules` — the deterministic **understand** primitive `readinessInputCompleteness` (over the seven readiness inputs: captured / missing / **blocking-missing** / `canDiagnose` / `completionPct`; credit score non-blocking, the other six required to mirror the store's assessment precondition; fails closed; registry-tracked with the nine-stage loop vocabulary). Plus a compact `ResolutionReadout` + pure `buildResolutionReadout` in `@aflo/shared/domain` that **composes already-verified facts and never recomputes them** (recorded diagnosis verbatim, engagement, utilization/DTI kernels for the organize snapshot, document-readiness counts, primary goal) with rule-version provenance — deliberately distinct from the staff `ClientDetail` UI aggregate and the client `PortalView`. Read-only, no AI, no mutation, no new sensitive data; store/UI wiring is a follow-up. 8 rules + 10 shared tests. **Merged (PR #48).**
- **Slice Y — Resolution readout wiring**: wired the Concierge substrate into the app. Read-only `store.resolutionReadoutFor(orgId, clientId, now)` composes the client's already-recorded facts via `buildResolutionReadout` (fail-closed org scope; mutates nothing, emits no event, writes no audit — asserted). Staff-facing **"Resolution readout"** card on client detail rendering understand (captured/missing inputs + completeness meter + `canRunDiagnosis`), diagnose (recorded stage + binding blocker + proposed next action), organize (DTI / utilization / monthly debt), and rule-version provenance. 7 store tests + e2e. **Merged (PR #49).**
- **Slice Z — credit-data store/UI wiring** (ADR-0013, wiring the Slice V interfaces): async `store.creditReportSummaryFor(orgId, clientId, now)` routes through the provider-neutral seam (synthetic `MockCreditDataProvider`, `isProduction` always false — no bureau) then `summarizeCreditReport`. Fail-closed org scope; **consent-gated** on `data_processing` (absent consent → `consent_required`, never the data; unknown subject → `no_report`); a **pure read** that mutates nothing and **never** writes the manual `CreditProfile` or the readiness inputs (readiness still reads only `CreditProfile` via `toReadinessFacts`). Staff **"Credit report (synthetic)"** card clearly labeled (Synthetic badge; "not a bureau report"; "never auto-updates the credit profile or the readiness assessment"). Synthetic reports seeded for c-whitaker/c-solomon (`source: mock`, `subjectRef` = client id, never an SSN); `data_processing` consent seeded for the demo. 7 store tests + e2e (populated + consent-gated). **Merged (PR #50).**
- **Slice AA — Opportunity & Risk Intelligence registry** (delivery #19, roadmap §4, ADR-0014): `@aflo/opportunity-intelligence` activated from stub — `opportunity.v1.0.0` deterministic trusted-source registry (`TRUSTED_SOURCES`: CFPB/HUD/IRS/CA-HCD; only these may back a notice), `OpportunityNotice` model (publication/effective/expiration dates, jurisdiction, eligibility *fields*, source citation, `verifiedEligibility` always false). **Safe-language boundary as code**: `validateOpportunityLanguage` rejects second-person eligibility/qualify/entitlement/approval claims, guarantees, and any dollar figure; `toClientSafeSummary` composes the only client-facing text from a fixed hedged template ("…may relate to your profile — review the official eligibility terms…") and **throws** rather than emit an unsafe claim. `matchNoticeToProfile` = deterministic **surface-worthiness** (jurisdiction/freshness/goal alignment, fail-closed) — never an eligibility determination. Human-review gate (`REVIEW_REQUIRED_CATEGORIES`, `requiresHumanReview`) for legal/claims notices. `validateOpportunityRegistry` CI guard + negative-fixture tests. No AI, no external calls, seed-only. A 3-lens adversarial review found 8 boundary gaps — all fixed (eligibilityFields now screened, deny-list broadened to catch evasions + dollar variants, review-gate enforced inside `toClientSafeSummary`, citation-url origin check, fail-closed expiration, deep-frozen citation). 19 tests. **Merged (PR #51).**
- **Slice AB — opportunity store/UI wiring**: read-only `store.opportunityNoticesFor(orgId, clientId, now, jurisdiction="US")` runs `matchNoticeToProfile` against the client's goal categories + jurisdiction (federal default; state unknown ⇒ state programs stay unsurfaced, fail-closed), keeps only `relevant` notices, and renders each via `toClientSafeSummary`. **Legal/claims notices (`requiresReview`) carry `clientSafe: null`** — shown to staff, never auto-projected to a client (roadmap §4); a notice that can't render safe is dropped. Org-scoped, no mutation/events/audit. Staff **"Opportunity notices"** card: hedged client-safe message + eligibility-fields-to-check + official-source link for ordinary notices; a "Staff review required" state for legal/claims. 6 store tests + e2e. **Merged (PR #52).**
- **Slice #4 — RLS org-isolation, runtime-verified** (delivery #4, tenant-isolation boundary): migration `0003` ENABLEs+FORCEs RLS on all 26 tenant tables with a fail-closed `org_isolation` policy, hardened to `nullif(current_setting('app.current_org_id', true), '')::uuid` so both an unset AND a cleared-empty context degrade to zero rows (never `''::uuid` errors). **Runtime-verified credential-free**: `rls-runtime.test.ts` applies every migration to in-memory Postgres (PGlite), `SET ROLE`s to a **non-superuser** role (so RLS actually applies), and proves list isolation, direct-id isolation (incl. foreign-keyed `clients`), `WITH CHECK` write rejection, cross-org UPDATE touching zero rows, and fail-closed on unset+empty context. `rls.test.ts` still derives the tenant set from the schema so a new tenant table without RLS fails CI. Additive, never applied to a live DB. The "unproven tenant isolation" stop-condition is resolved. 46 database tests. **Merged (PR #45).**
- **Slice — durable transactional outbox repository** (delivery #3/#8, ADR-0008): first PostgreSQL repository behind an interface. `OutboxRepository` (`@aflo/shared`) + `DrizzleOutboxRepository` (`@aflo/database`) drive every state change through the `outbox.v1.0.0` kernel and claim with `FOR UPDATE SKIP LOCKED`. **Crash recovery**: new deterministic `expireLock` transition + `reapExpired` return a record stranded in `processing` by a crashed worker to the retry path once its visibility window elapses (or dead-letter if attempts are spent) — restoring at-least-once delivery across crashes. Canonical jsonb payload + exact timestamp round-trip; idempotent enqueue on `event_id`; migration `0004` (additive: `max_attempts`, `processed_at`). Adversarial persistence review before merge (HIGH crash-strand + 2 test-honesty findings fixed). **Tested RLS deployment contract**: the worker drains all orgs so it MUST use an RLS-bypassing role — a test proves a non-privileged role with no org context drains nothing. Proven credential-free on PGlite. `@aflo/shared` promoted to a runtime dep of `@aflo/database` (cycle-free). 59 database + 189 shared tests. **Merged (PR #53).**
- **Slice — transactional-outbox producer (write half)** (delivery #3/#8, ADR-0008): `commitWithOutbox(db, work)` runs a state change and the outbox events it emits in ONE transaction — both commit or neither does. Proven on PGlite for all three paths: happy commit, rollback when `work` throws (no orphaned events), and rollback when an outbox insert fails (no orphaned state), plus `event_id` idempotency across retried units of work. `insertOutboxRecord(qb, record)` extracted so producers enqueue on the same tx as their domain writes. Completes the outbox story (producer + the #53 consumer). 63 database tests. **Merged (PR #55).**
- **Slice — database environment validation** (delivery #3, Phase 1): `@aflo/database` `getDatabaseConfig(env, { requireDirectUrl })` validates and parses the Neon two-URL setup — `DATABASE_URL` (pooled runtime) + `DIRECT_DATABASE_URL` (direct/migrations) — **failing closed** with one aggregated `DatabaseConfigError` listing every problem (missing, non-URL, wrong scheme, no database name). `isDatabaseConfigured` is the non-throwing check the app uses to pick mock-vs-Neon backing. Nothing connects; it's the credential-free precondition for the connection factory. 9 tests. **Merged (PR #56).**
- **Slice — synthetic-data seed-loader (schema⟷domain fidelity)** (delivery #3, Phase 1): `loadSyntheticCore(db, seed)` materializes the CORE domain data (organizations, clients, financial_profiles, credit_profiles, goals) into Drizzle in one transaction, FK-ordered — the fidelity keystone the mock-vs-Postgres parity tests build on. Surfaced + handled real impedance: the synthetic prototype ids are slugs (`c-solomon`) but production columns are `uuid`, so `slugToUuid` deterministically remaps every id + reference (relationships preserved); `organization_id` is injected into sub-records from the owning client; `bigint` cents round-trip exactly, `numeric(4,3)` on-time-rate as a decimal string. Staff→users/members, encrypted PII, and readiness_assessments are documented follow-ups. Proven credential-free on PGlite (round-trip counts + field fidelity + injection + the ≤1-primary-goal invariant). 10 tests. **Merged (PR #57).**
- **Slice — secure-messaging kernel + client-safe projection** (delivery #7, loop feature; founder-directed pivot to user-facing loop): `messaging.v1.0.0` deterministic kernel — `validateMessageDraft` (fail-closed: missing sender / closed thread / empty / over the 5000-char cap → reason codes) + `transitionThread` (open⇄closed legal moves only); registered in the rule registry. Domain `toClientThreadView` is the **safety boundary**: a client thread is built ONLY from `Message`s (internal `AdminNote`s are a separate model, structurally unable to appear) and strips all internal fields — the client sees "you"/"advisor", body, and time, never a staff id or tenant metadata (leak-guard tested). First slice of the messaging feature; store wiring + staff/portal UI are the follow-ups. 16 tests. **Merged (PR #58).**
- **Slice — secure-messaging store wiring** (delivery #2 of the production directive): `AfloStore` now holds tenant-scoped `ConversationThread`/`Message` state (+ synthetic seed). `openThread` / `postReply` are rules-gated (`validateMessageDraft`), authorize the sender (staff must be an org member; a client may post ONLY to their own thread), and each append emits a `MessagePosted` outbox event — **carrying no body** (ids + sender role only, so sensitive content never enters the outbox) — plus an audit record; `closeThread`/`reopenThread` gate on `transitionThread`. Reads are org-verified: `conversationsFor` / `messagesForThread` (staff) and `clientConversationsFor` (the client-safe `toClientThreadView` projection). Fail-closed throughout: a foreign org gets `THREAD_NOT_FOUND`, a cross-client post gets `NOT_THREAD_CLIENT`, and a denied write leaves no audit or event. `MessagePosted` appended to the closed event catalog (29 events; `conversation` aggregate). 15 store tests (+ leak-guard + no-body-in-event + a two-org cross-tenant isolation proof); adversarially reviewed (isolation + no-leak). **Merged (PR #59).**
- **Slice — secure-messaging UI** (delivery #2 cont.): staff **"Secure messages"** card on client detail (raw thread view via `conversationsFor`/`messagesForThread`, sender shown by name) and a client-portal **"Secure messages"** card rendered from the `PortalView.conversations` projection (client-safe `toClientThreadView` — "You"/"Advisor" only). e2e proves the staff thread renders AND the portal never leaks a staff id. Read-only display (interactive send is the next slice). 2 e2e (50 total). **Merged (PR #60).**
- **Slice — Golden Key brand infrastructure** (production directive "Brand implementation", ADR-0015): centralized `OrganizationBrand` component (config-driven per-surface logo sources, `GOLDEN_KEY_THEME` tokens Onyx/Charcoal/Gold/White/Soft-gray, tenant-vs-ΛFLO-platform separation, `headingLevel` for accessible headings, future-SVG-swap-ready). **Renders an accessible text fallback** (monogram + wordmark, no layout shift) because the raster PNGs are not in the repo — wired into the sign-in shell, replacing the hardcoded mark. `apps/web/public/brands/golden-key-wealth/README.md` documents the required `primary-dark.png`/`primary-light.png` (temporary rasters; final SVGs still required — do NOT auto-trace). ADR-0015 records the placement decision (apps/web now → packages/ui when the worker email/report surfaces need it). Gates + 50 e2e green. **Merged (PR #61).** ⛔ Still needs the brand PNG assets (`primary-dark.png`/`primary-light.png`) to switch off the text fallback.
- **Slice — Golden Key brand guide captured** (founder-supplied brand guide 2026-07-19): aligned `GOLDEN_KEY_THEME` to the five official swatches (gold `#D4AF37`, white `#FFFFFF`, onyx `#0A0A0A`, charcoal `#1E1E1E`, gray `#6B6B6B`), added `GOLDEN_KEY_VOICE` (tagline "Strategy. Clarity. Freedom.", positioning, values Strategic/Trusted/Innovative/Empowering), rendered the tagline on the sign-in shell, and documented the full guide (mark, palette, Futura PT/Avenir Next typography, voice) in `BRAND_SYSTEM.md §2`. **Brand SPEC now provided** — palette/type/voice no longer blockers. ⛔ Remaining: export the logo asset files (dark/light PNG or SVG) from the guide to switch off the text fallback, and provision the Futura PT/Avenir Next font kit (Adobe Fonts). Full black/gold tenant-surface re-theme is a deliberate follow-up, off the auth critical path. _In PR._
- **Slice — interactive secure messaging** (Phase 1 messaging, ADR-0016): completes the two-way messaging loop. Staff **reply** + **open a new conversation** (client-detail card); the client **replies** from the portal. Identity (org + actor/client) comes ONLY from the server session; the store re-verifies thread ownership and rejects any denial fail-closed (traceless — no message/event/audit). The client-safe projection stays **id-free**: a client reply targets a thread by its **position** in the client's own conversation list, re-resolved server-side, so the browser never holds an internal thread id (ADR-0016 records the decision + rejected alternatives). Adversarially reviewed (isolation airtight both directions, projection id-free, closed/empty/oversized fail-closed); the review's MEDIUM (docs overclaimed denial-auditing) was fixed by correcting the ADR/comments to the tested traceless-denial behavior. 3 e2e (53 total). **Merged (PR #62).**
- **Slice — message read receipts + unread indicators** (Phase 1 messaging, production-conversion directive): new **`MessageRead`** event (30 events; counts + roles only, never a body); store `markThreadRead` (marks only the counterparty's unread messages, idempotent traceless no-op when nothing unread, org/ownership re-verified), `unreadCountForStaff`, `unreadCountForClient`; client-safe `ClientThreadView.unreadCount` (a number). Staff + client UIs show an "N new" badge and a "Mark as read" control (client mark-read uses the same id-free session-resolved index as replies). Cross-tenant / wrong-client mark-read denied + traceless. Unit-tested (counts, idempotency, MessageRead payload, no-body, cross-tenant deny) + read-only e2e. Adversarially reviewed (SAFE TO MERGE — isolation both directions, read-state direction, id-free projection, event wiring, audit all verified). **Merged (PR #63).**
- **Slice — production runtime fail-closed contract** (production-conversion **Phase 2**, ADR-0017): pure, self-contained runtime contract in `@aflo/shared/src/runtime` — `resolveRuntimeMode` (`test|development|preview|production`, where **production is only ever entered by an explicit `APP_ENV=production`**, never inferred from `VERCEL_ENV`/`NODE_ENV` so the live prototype can't silently fail closed), `resolveRuntimeConfig` (permissive outside production; in production **fails closed** on every forbidden fallback — demo auth, mock/in-memory repos, synthetic seed, mock email, public storage — and every missing required integration — both DB URLs + non-preview branch, Clerk keys, private storage, encryption-key ref, worker secret, Sentry DSNs), `assertRuntimeReady` (throws `RuntimeConfigError`), and `describeRuntimeReadiness` (non-secret booleans + modes). New **`GET /api/health`** reports the non-secret readiness snapshot (503 when degraded) — reports only, never exposes a secret value. This is the NON-NEGOTIABLE "never silently fall back to demo/mock in production" guard as executable, tested code. 15 runtime tests + health e2e. Adversarially reviewed (SAFE TO MERGE — no false-negative bypass, no false-positive on the live prototype, no secret leak, full must-reject coverage). **Merged (PR #64).**
- **Slice — runtime boot enforcement + preview-DB hardening** (production-conversion Phase 2 cont., ADR-0017 follow-ups): `apps/web/src/instrumentation.ts` `register()` now calls `assertRuntimeReady(process.env)` at server startup so the fail-closed guarantee is **active, not advisory** — **verified end-to-end**: `APP_ENV=production` with nothing configured → `register()` throws → Next refuses to prepare the server → every request 500s (no demo/mock served); `APP_ENV` unset → `development` → boots normally (55 e2e pass). Preview-branch detection **strengthened**: an explicit `DATABASE_BRANCH` is now authoritative (`isPreviewDatabase` — `preview` rejected in prod even with an opaque Neon host; `main` overrides a preview-looking URL), URL heuristic kept as fallback. Worker boot enforcement **deferred** (stub with no `@aflo/shared` JS build; folds into its credential-blocked real-operation slice). 16 runtime tests. _In PR._
- Founder-action-gated (credentials/accounts, not code): Neon connection + repository swap (`DATABASE_URL`), outbox worker (needs the shared DB), Clerk activation (keys), Resend activation (keys), Stripe test-mode billing (keys + approved packages). Remaining credential-free modules: admin settings layer, engagement/retention analytics, observability instrumentation.

## Next ⏭ (forward order — the persistence pivot is the decisive milestone)

The synthetic-module surface is broad; the next decisive work moves it from in-memory demonstration to durable, restart-safe operation. Credential-free work continues in parallel.

1. **Durable-persistence layer (credential-free, via PGlite)** — with the outbox repository (#53) and RLS runtime proof (#45) landed, continue porting the store behind interfaces: Drizzle-backed **read repositories** (Dashboard/Client/Portal) with **mock-vs-PGlite parity tests**, and the **transactional producer** (state row + outbox row committed atomically, ADR-0008). Each slice proven credential-free on PGlite so the swap is safe the moment `DATABASE_URL` lands. **← next**
2. **Live Neon connection + store swap** — **env validation ✅ (`getDatabaseConfig`/`isDatabaseConfigured`, fail-closed, credential-free)**; the connection factory (driver + pool), applying migrations `0000`–`0004` to the Neon `dev` branch, and swapping the store onto the parity-proven repositories remain **gated on `DATABASE_URL`.**
3. **Clerk activation (order #7)** and **Railway outbox worker process (order #8)** — the outbox repository is merged (#53); the worker process + Clerk activation are gated on credentials / the shared database.
4. **Behavioral Support Engine (order #18, opt-in) 👤** — sensitive (behavioral profiling); **awaits founder scoping before any build.**
5. **Real communications (Resend/Twilio), Stripe test mode, and document storage** — each gated on credentials/accounts.
6. **Loop-completion features buildable now on the store** (advance the "one user, whole system" thesis with synthetic data): secure staff↔client messaging, the Resolution Concierge guided flow (one question at a time, deterministic, staff handoff), internal calendar/`.ics`, and end-user self-registration/onboarding.

## Phased later (gated — see `PARTNER_ORCHESTRATION_ROADMAP.md`)

- **After pilot:** real affiliate integrations · bureau-data provider adapter · credit monitoring/alerts · opportunity & regulatory intelligence feed.
- **After commercial + compliance validation:** embedded credit-builder applications · AFLO-branded secured card (sequence: referrals → embedded apps → co-brand pilot → branded card) · deposit/savings-linked products · interchange-based revenue model.

## Blocked ⛔ / Founder decision required 👤

- **Vercel / Railway / Stripe activation + Clerk/Resend credentials** — requires founder account authorization and environment secrets (charter stop conditions #3, #7). Configuration is prepared in-repo; activation waits on the founder connecting accounts and providing secrets via the platform dashboards (never in the repo).
- ~~Auth provider~~ ✅ **decided 2026-07-18: Clerk** (ADR-0006 Accepted) — implementation behind `packages/auth` adapter when the auth slice lands; credentials prepare-only.
- **Production Stripe charges** ⛔ — disabled until valid credentials and founder-approved service packages exist; all billing work stays in test-mode / pure logic until then.
- **Golden Key workflow specifics** 👤 — `docs/research/GOLDEN_KEY_WORKFLOW_DISCOVERY.md` holds TBD questions for Natalia; assumptions stay configurable until answered.

## Security review required 🔒

- Any change to payment-webhook verification, authorization/tenant-isolation boundaries, or sensitive-data controls will be opened as a PR and held for founder review (never auto-merged).
- **RLS org-isolation (#45) merged** with runtime verification (PGlite non-superuser role) resolving the "unproven tenant isolation" stop-condition — the tenant-isolation boundary is additive defense-in-depth, never applied to a live DB, and adversarially reviewed. Future changes that _weaken_ isolation or sensitive-data controls still hold for founder review.

## Deferred 🕓

- Neon-backed repository implementations (ADR-0002 mock-first; swap behind unchanged interfaces).
- Real document storage + signed URLs, malware scanning (future requirement).
- Turborepo/Nx (deferred per ADR-0004).

## Deployment status

- **main**: contains the deployable monorepo through the full Drizzle persistence schema (PRs #41–#44), provider-neutral credit-data interfaces (#41), the vendor-discovery registry + disabled Experian adapter (#46), the Resolution Concierge substrate + credit-report card + Opportunity registry (#48–#52), the runtime-verified RLS org-isolation DDL (#45), and the durable transactional-outbox repository + migration `0004` (#53). Still mock-backed at runtime; migrations `0000`–`0004` are never-applied (no live Neon).
- **Vercel**: **connected** (project `aflo-web`, root `apps/web`) — per-PR preview deployments build and deploy on every push. Production promotion + env vars remain a founder action.
- **Railway**: not yet created — build/start commands and config documented; requires founder account. The worker stays inactive until the shared Neon database is connected.
- **Neon**: **provisioned by founder** (`main`/`preview`/`dev` branches exist — never recreate). The app is still mock-backed; no code connects until the Neon repository swap, and connection strings live only in provider dashboards (`DATABASE_URL`).
- **Repository visibility**: recommend setting the GitHub repo to **private** before real credentials, vendor details, or pilot config are added (a founder admin action in repo Settings → Change visibility). Everything committed today is synthetic — no secrets or real PII.
